# Database URL
# For SQLite in-memory (development only)
# DATABASE_URL is not needed for in-memory, but you can add it for production

# Settings Encryption Secret
# CRITICAL: Generate a strong 32-byte random string for production
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SETTINGS_ENCRYPTION_SECRET=your_settings_encryption_secret_here_change_this_in_production

# Base URLs
API_BASE_URL=http://localhost:3000
UI_BASE_URL=http://localhost:4200

# Application Port
PORT=3000

# Optional: Log Level (debug, info, warn, error)
LOG_LEVEL=info

# Note: All third-party API keys (Stripe, Twilio, SendGrid, PayPal) are now
# managed through the database via the Admin Settings UI. Only the
# SETTINGS_ENCRYPTION_SECRET needs to be in this .env file.
