{
  "name": "explore-ni-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "ui",
    "api"
  ],
  "scripts": {
    "start": "concurrently \"npm run start -w api\" \"npm run start -w ui\" \"npm run open:browser\"",
    "open:browser": "wait-on http://localhost:4200 && open-cli http://localhost:4200"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0",
    "open-cli": "^8.0.0"
  }
}